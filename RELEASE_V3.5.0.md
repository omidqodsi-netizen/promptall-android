# promptAll Android v3.5.0

## تغییرات اصلی

- جایگزینی تب «تنظیمات» در نوار پایین با «دسته‌بندی».
- صفحه مستقل دسته‌بندی‌ها با نمایش تمام دسته‌های دریافتی از API سایت.
- بخش «ترند روز» در بالای صفحه دسته‌بندی‌ها با داده‌های `trending-prompts`.
- پشتیبانی خودکار از دسته جدید پرامپت‌های ویدئویی از طریق API دسته‌بندی‌های سایت.
- فید مستقل برای هر دسته با صفحه‌بندی، Pull-to-refresh، علاقه‌مندی و کپی پرامپت.
- انتقال صفحه اطلاعات برنامه به آیکن تنظیمات در هدر صفحه دسته‌بندی‌ها.
- حفظ صفحه خانه، جست‌وجو، علاقه‌مندی و دکمه تصادفی بدون تغییر رفتاری.
- افزایش نسخه به `3.5.0` با `versionCode = 30500`.

## API

این نسخه endpoint جدیدی نیاز ندارد و از API فعلی استفاده می‌کند:

- `GET /wp-json/promptall/v1/categories`
- `GET /wp-json/promptall/v1/prompts?category={slug}`

ترند روز با slug زیر خوانده می‌شود:

- `trending-prompts`

## نکته انتشار

برای انتشار در بازار باید همان کلید امضای نسخه فعلی استفاده شود. Workflow پروژه نیز نام فایل‌های خروجی را برای v3.5.0 تولید می‌کند.
